# bootstrap-primer-theme

A very rough bootstrap theme inspired by githubs colors:
https://primer.style/primitives/colors

## Build it yourself

First, you need to install node and npm.

```
npm install
npm install -g sass
npm run css-compile
```
